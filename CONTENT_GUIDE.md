# Руководство по редактированию контента

Этот файл поможет вам быстро найти и изменить информацию на сайте.

## 1. Основные данные (Опыт, Проекты, Контакты)
Все основные данные хранятся в одном файле:
📂 **Путь:** `constants.ts`

Откройте этот файл в редакторе кода. Вы увидите следующие разделы:

### `JOBS` (Опыт работы)
Список ваших мест работы.
```typescript
export const JOBS: Job[] = [
  {
    id: "nipigas", // Уникальный ID (не меняйте, если не уверены)
    company: "NIPIGAS", // Название компании
    title: "Chief Specialist", // Должность
    period: "2021 — Present", // Период
    // ...
    description: [
      "Здесь пункты описания вашей работы.",
      "Каждый пункт в кавычках и через запятую."
    ],
    technologies: ["Excel", "Python"] // Технологии внизу карточки
  },
  // ... другие места работы
];
```

### `PROJECTS` (Проекты)
Список проектов внизу страницы.
```typescript
export const PROJECTS: Project[] = [
  {
    id: "project1",
    title: "Название проекта",
    description: "Описание проекта...",
    tools: ["React", "D3.js"], // Инструменты
    link: "https://..." // Ссылка (если есть)
  },
];
```

### `D3_DATA` (Навыки в облаке тегов)
Здесь редактируются навыки, которые отображаются в блоке "Core Competencies".
*   `group: 1` — Engineering & Design
*   `group: 2` — Data & Automation
*   `group: 3` — Management & Strategy

```typescript
nodes: [
  { id: "Gas Pipelines", group: 1, radius: 25 },
  { id: "Python", group: 2, radius: 15 },
  // Добавляйте новые строки сюда
],
```

### `SOCIAL_LINKS` (Контакты)
Ваши ссылки и телефон.
```typescript
export const SOCIAL_LINKS = {
  linkedin: "...",
  email: "...",
  phone: "+7 (999) 000-00-00" // Ваш телефон
};
```

---

## 2. Текст "Обо мне" (About)
Текст приветствия и раздела "About" находится в главном файле приложения.
📂 **Путь:** `App.tsx`

Найдите строки (примерно 109-119), где написан текст:
```tsx
<p>
  I am a highly qualified energy infrastructure expert based in <span className="text-teal-300 font-medium">Belgrade, Serbia</span>...
</p>
```
Вы можете менять текст внутри тегов `<p>...</p>`.
Чтобы выделить слово цветом, оберните его в:
`<span className="text-teal-300">Ваше слово</span>`

---

## 3. Как применить изменения?
После сохранения файла (`Ctrl+S` или `Cmd+S`), сайт в браузере обновится автоматически (если запущен `npm run dev`).

---

## 4. Структура проекта
```
Dun4ev_site_r1/
├── components/          # Компоненты React
│   ├── SkillCloud.tsx  # Облако навыков
│   ├── SkillRadar.tsx  # Радар навыков
│   └── ...
├── constants.ts        # ⭐ Основные данные (JOBS, PROJECTS, SKILLS)
├── App.tsx            # ⭐ Главный файл приложения
├── index.tsx          # Точка входа
└── CONTENT_GUIDE.md   # Этот файл
```
